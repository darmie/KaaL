[package]
name = "test-cap-revoke"
version = "0.1.0"
edition = "2021"

# Empty workspace table to prevent this from being part of parent workspace
[workspace]

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
codegen-units = 1
strip = true
